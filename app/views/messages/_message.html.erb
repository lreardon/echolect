<div class="mb-3">
	<div class="flex flex-row gap-5">
		<p class="text-gray-500"><%= message.user.name %></p>
		<p class="text-gray-500"><%= message.created_at %></p>
	</div>
	<p><%= message.body %></p>
</div>