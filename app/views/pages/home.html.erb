<div class="px-4 flex flex-col items-center w-full gap-10">
  <%= render partial: 'chats/chats', locals: { chats: current_user.chats } %>
  <% if current_user.active_affiliation? %>
    <%= render partial: 'course_offerings/course_offerings' %>
  <% end %>
</div>
